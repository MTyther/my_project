Quick setup — if you’ve done this kind of thing before
or	
HTTPS
SSH
https://github.com/martinty35/my_project.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# my_project" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/martinty35/my_project.git
git push -u origin master
…or push an existing repository from the command line
git remote add origin https://github.com/martinty35/my_project.git
git push -u origin master
…or import code from another repository
You can initialize this repository with code from a Subversion, Mercurial, or TFS project.
_________________________________________________________________________________________________________________________________
# Making changes to file readme.txt in local repository
# just type the filename with extension
>> readme.txt
# Filename shows up with status in red because of uncommitted changes with git status command.
>> git status
# commit changes to master from local
>> git commit -m "write another note of change made to file here"
# can include this line here if filepath was not explicitly established:-
# git remote add origin https://github.com/martinty35/my_project.git
>> git push -u origin master

# To create a new empty pycharm file...
>> copy con hello_world.py
>> ctrl z (cr)
__________________________________________________________________________________________________________________________________
Quick setup — if you’ve done this kind of thing before
or	
HTTPS
SSH
https://github.com/martinty35/my_project.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# my_project" >> README.md
git init
git remote add origin https://github.com/martinty35/my_project.git
git remote -v
copy con readme.txt
ctrl z and enter
git status
git add readme.txt
git commit -m "first commit"
git push -u origin master
…or push an existing repository from the command line
git remote add origin https://github.com/martinty35/my_project.git
git push -u origin master
…or import code from another repository
You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

